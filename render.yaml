services:
  - type: web
    name: agri-ai
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run app.py --server.port=${PORT:-8501} --server.headless true"
    envVars:
      - key: GROQ_API_KEY
        sync: false
