services:
  - type: web
    name: agri-ai
    env: python
    pythonVersion: 3.12
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: GROQ_API_KEY
        sync: false
